#导入 洛书/引脚
#导入 洛书/标准流
#类 风扇
    #标准流 [此类].程序
    #引脚 [此类].风扇
    #方法 配置(引脚)
        [此类].风扇.设定(引脚,"1")
        #回收 引脚
    #方法 启动()
        [此类].风扇.写入("1")
    #方法 关闭()
        [此类].风扇.写入("0")
    #方法 转动(毫秒)
        [此类].风扇.写入("1")
        [此类].程序.等待(毫秒)
        [此类].风扇.写入("0")
        #回收 毫秒